name: testing

on:
  workflow_call:
    inputs:
      variables:
        required: true
        type: string
      build-config:
        type: string
jobs:
  testing-further:
    runs-on: ubuntu-latest
    name: testing-further ${{input.build-config }}
    steps:
    - run: echo "testing"
  further-release:
    runs-on: ubuntu-latest
    name: further-release ${{input.build-config }}
    steps:
    - run: echo "testing"